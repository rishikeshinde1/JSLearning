var firstName = 'Ajay';
var middleName = 'Vijay';
var lastName ='Patil';

console.log('---------------------------------------------------------------------------');

console.log('-----First Name is:', firstName, '-----data type is:',typeof(firstName));
console.log('-----Middle Name is:', middleName, '-----data type is:',typeof(middleName));
console.log('-----Last Name is:', lastName, '-----data type is:',typeof(lastName));

console.log('---------------------------------------------------------------------------');

var age = 32;
var pincode = 451551;
var mobileNumber = 8794512214;

console.log('-----Age is:', age, '-----data type of age is:',typeof(age));
console.log('-----Pincode is:', pincode, '-----data type of pincode is:',typeof(pincode));
console.log('-----Mobile Number is:', mobileNumber, '-----data type of Mobile Number is:',typeof(mobileNumber));

console.log('---------------------------------------------------------------------------');

var isWorking = true;
var isMarried = false;
var isStudying = true;

console.log('-----Working status:', isWorking, '-----data type is:',typeof(isWorking));
console.log('-----Married status:', isMarried, '-----data type is:',typeof(isMarried));
console.log('-----Study status:', isStudying, '-----data type is:',typeof(isStudying));

console.log('---------------------------------------------------------------------------');

console.log('Difference Between null vs undefined - ');
console.log('If we want to empty the variable value we need to assign the value null to that variable, by default when we declare the variable without intializing the value to any variable is undefined');
console.log('The datatype of null is object where as datatype of undefined variable is undefined.');